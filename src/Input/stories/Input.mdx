import { Canvas, Meta } from '@storybook/blocks';

import * as InputStories from './Input.stories';

<Meta of={InputStories} />

# Input

The input component is used to capture text-based user input.

<Canvas of={InputStories.Simple} />

## Usage

```jsx

import { Input } from 'react-barebones-ts';

<Input placeholder="Placeholder text" />

```

## Props

### name

The name of the input.

```jsx
<Input name="name" />
```

### value

The value of the input.

```jsx

<Input value="Value" />

```

### onChange

The function to call when the input value changes.

```jsx

const [fieldName, setFieldName] = useState('')

handleSetFieldName = (e) => {
    setFieldName(e.target.value)
}

<Input value={fieldName} onChange={handleSetFieldName} />

```

### type

The type of input to render.

```jsx

<Input type="password" />

```

### label

The label to display above the input. Can be a React Node or Component

```jsx

<Input label="Email" />

```
<Canvas of={InputStories.Label} />

### disabled

Whether the input is disabled.

types: `boolean`

```jsx

<Input disabled />

```

<Canvas of={InputStories.Disabled} />
